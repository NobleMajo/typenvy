export default {
    PORT: 8080 as number,
    SECRET: "test" as string,
    POSTGRES_HOST: "localhost" as string,
    POSTGRES_PORT: 5432 as number,
    POSTGRES_USER: "admin" as string,
    POSTGRES_PASSWORD: "postgres" as string,
    POSTGRES_DB: "default" as string,
}